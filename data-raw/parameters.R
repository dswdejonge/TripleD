# Required station fields
required_station_fields <- c(
  "StationID",
  "Date",
  "Lat_start(DD)",
  "Lon_start(DD)",
  "Lat_stop(DD)",
  "Lon_stop(DD)",
  "Blade_depth(cm)",
  "Blade_width(cm)"
)

alternative_gps_fields <- c(
  "Lat(DD)",
  "Lon(DD)",
  "Track_dist(m)"
)
